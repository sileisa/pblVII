@model IEnumerable<PBLVII.Models.Programador>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nivel)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nivel)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.ProgramadorId }) |
            @Html.ActionLink("Buscar Atividades", "RelatorioAtividadesPorProgramador", new { id = item.ProgramadorId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ProgramadorId })

        </td>
    </tr>
}

</table>
<div>
    @Html.ActionLink("Buscar Tarefas Por Programador", "BuscarAtividades")
</div>